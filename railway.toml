[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.pefy-capability-runtime"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
