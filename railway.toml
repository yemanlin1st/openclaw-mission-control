[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.pefy-graphify-runtime"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
